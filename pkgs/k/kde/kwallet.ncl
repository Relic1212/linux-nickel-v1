{
  lib,
  pixman,
  expat,
  libffi,
  libevdev,
  mtdev,
  pango,
  cairo,
  libpng,
  freetype,
  fontconfig,
  zlibNg,
  harfbuzz,
  glib,
  hwdata,
  linuxHeaders,
  libjpegTurboStatic,
  libspng,
  file,
  libwebp,
  dbusForQt,
  qtdeclarative,
  qtbase,
  qtshadertools,
  qtwayland,
  mesa2,
  pkgconf,
  libelf,
  libdrm,
  pythonMako,
  pythonPackaging,
  pythonMarkupsafe,
  libpciaccess,
  bison,
  flex,
  wayland,
  waylandProtocols,
  m4,
  pyyaml,
  llvmConfigStatic,
  spirvTools,
  spirvLlvmTranslator,
  jsonC,
  pcre2,
  libxkbcommon,
  openssl,
  kmod,
  zstd,
  xz,
  eudev,
  libinput,
  ecm,
  kcoreaddons,
  kcodecs,
  kconfig,
  kguiaddons,
  ki18n,
  gettext,
  karchive,
  bzip2,
  kservice,
  kwidgetsaddons,
  kcolorscheme,
  kconfigwidgets,
  ksolid,
  utilLinuxWeak,
  kcrash,
  kbookmarks,
  kcompletion,
  kiconthemes,
  breezeIcons,
  kitemviews,
  kjobwidgets,
  knotifications,
  vala,
  libtool,
  libogg,
  libvorbis,
  libcanberra,
  kwindowsystem,
  kauth,
  kdbusaddons,
  kded,
  llvmFull,

  kwallet | lib.CmakePackage = {
    generator = "Ninja",
    singleSource = {
      uri = "https://invent.kde.org/frameworks/kwallet/-/archive/v6.11.0/kwallet-v6.11.0.tar.gz",
      sha256sum = "4b3ff3b81b0a61fd74bac5de48240d93453ab9e82517dc2b115cd8582f21b3c4"
    },
    llvmProvider = lib.singleBiMap llvmFull,

    biDrvs =
      lib.biMap [
        pixman,
        expat,
        libffi,
        libevdev,
        mtdev,
        pango,
        cairo,
        libpng,
        freetype,
        fontconfig,
        zlibNg,
        harfbuzz,
        glib,
        hwdata,
        linuxHeaders,
        libjpegTurboStatic,
        libspng,
        file,
        libwebp,
        dbusForQt,
        qtdeclarative,
        qtbase,
        qtshadertools,
        qtwayland,
        mesa2,
        # pkgconf,
        libelf,
        libdrm,
        pythonMako,
        pythonPackaging,
        pythonMarkupsafe,
        libpciaccess,
        bison,
        flex,
        wayland,
        waylandProtocols,
        m4,
        pyyaml,
        llvmConfigStatic,
        spirvTools,
        spirvLlvmTranslator,
        ###
        jsonC,
        pcre2,
        libxkbcommon,
        openssl,
        kmod,
        zstd,
        xz,
        eudev,
        libinput,
        ecm,
        kcoreaddons,
        kcodecs,
        kconfig,
        kguiaddons,
        ki18n,
        gettext,
        karchive,
        bzip2,
        kservice,
        kwidgetsaddons,
        kcolorscheme,
        kconfigwidgets,
        ksolid,
        utilLinuxWeak,
        kcrash,
        kbookmarks,
        kcompletion,
        kiconthemes,
        breezeIcons,
        kitemviews,
        kjobwidgets,
        knotifications,
        vala,
        libtool,
        libogg,
        libvorbis,
        libcanberra,
        kwindowsystem,
        kauth,
        kdbusaddons,
        kded,
      ],
    buildEnv = {
      EXTRA_LDFLAGS = [
        "$(pkg-config --libs --static gbm glesv2 egl dri wayland-client)",
      ]
    },
    cmakeExtraArgsRecord = {
      # "-DECM_DIR"="/usr/usr/share/ECM/",
      "-DCMAKE_MODULE_LINKER_FLAGS:STRING" = "\"-static --static -fuse-ld=lld -stdlib=libc++ -rtlib=compiler-rt -unwindlib=libunwind -lc++ -lglapi -ldrm $(pkg-config --libs --static dri glesv2 gbm egl wayland-client)\""
    },
  }
}
& {
  lib,
  pixman,
  expat,
  libffi,
  libevdev,
  mtdev,
  pango,
  cairo,
  libpng,
  freetype,
  fontconfig,
  zlibNg,
  harfbuzz,
  glib,
  hwdata,
  linuxHeaders,
  libjpegTurboStatic,
  libspng,
  file,
  libwebp,
  qtdeclarative,
  qtbase,
  qtshadertools,
  qtwayland,
  mesa2,
  pkgconf,
  libelf,
  libdrm,
  pythonMako,
  pythonPackaging,
  pythonMarkupsafe,
  libpciaccess,
  bison,
  flex,
  wayland,
  waylandProtocols,
  m4,
  pyyaml,
  llvmConfigStatic,
  spirvTools,
  spirvLlvmTranslator,
  jsonC,
  pcre2,
  libxkbcommon,
  openssl,
  kmod,
  zstd,
  xz,
  eudev,
  libinput,
  llvmFull,

  ecm | lib.CmakePackage = {
    generator = "Ninja",
    singleSource = {
      uri = "https://invent.kde.org/frameworks/extra-cmake-modules/-/archive/v6.11.0/extra-cmake-modules-v6.11.0.tar.gz",
      sha256sum = "3c7bdaf5947041d5397b3123e57e9a5ecd9ccab514e448ad8f1959cda1117fde"

      # uri = "https://invent.kde.org/frameworks/extra-cmake-modules/-/archive/v6.10.0/extra-cmake-modules-v6.10.0.tar.gz",
      # sha256sum = "6025709712e075f06c3b9eebfffc50ce31605712d8947c748d9b2241e915f595"
    },

    llvmProvider = lib.singleBiMap llvmFull,

    biDrvs =
      lib.biMap [
        pixman,
        expat,
        libffi,
        libevdev,
        mtdev,
        pango,
        cairo,
        libpng,
        freetype,
        fontconfig,
        zlibNg,
        harfbuzz,
        glib,
        hwdata,
        linuxHeaders,
        libjpegTurboStatic,
        libspng,
        file,
        libwebp,
        qtdeclarative,
        qtbase,
        qtshadertools,
        qtwayland,
        mesa2,
        # pkgconf,
        libelf,
        libdrm,
        pythonMako,
        pythonPackaging,
        pythonMarkupsafe,
        libpciaccess,
        bison,
        flex,
        wayland,
        waylandProtocols,
        m4,
        pyyaml,
        llvmConfigStatic,
        spirvTools,
        spirvLlvmTranslator,
        ###
        jsonC,
        pcre2,
        libxkbcommon,
        openssl,
        kmod,
        zstd,
        xz,
        eudev,
        libinput,
      ],
    buildEnv = {
      EXTRA_LDFLAGS = [
        "$(pkg-config --libs --static gbm glesv2 egl dri wayland-client)",
      ]
    },
    cmakeExtraArgsRecord = {
      "-DBUILD_WITH_QT6" = true,
      "-DCMAKE_MODULE_LINKER_FLAGS:STRING" = "\"-static --static -fuse-ld=lld -stdlib=libc++ -rtlib=compiler-rt -unwindlib=libunwind -lc++ -lglapi -ldrm $(pkg-config --libs --static dri glesv2 gbm egl wayland-client)\""
    },
  }
}
& (import "./kwin.ncl")
& (import "./kf6.ncl")
