diff --git a/src/app/application.cpp b/src/app/application.cpp
index 845ae8841..8bf65c2b5 100644
--- a/src/app/application.cpp
+++ b/src/app/application.cpp
@@ -268,7 +268,7 @@ void Application::run() {
   runStartupPhase("telemetry enqueue",
                   [this]() { m_telemetryService.maybeSend(m_configService, m_httpClient, m_wayland); });
 
-  runStartupPhase("malloc_trim", []() { malloc_trim(0); });
+  // runStartupPhase("malloc_trim", []() { malloc_trim(0); });
 
   m_trayInitTimer.start(std::chrono::milliseconds(500), [this]() { startTrayService(); });
   m_polkitInitTimer.start(std::chrono::milliseconds(0), [this]() { syncPolkitAgent(); });
@@ -983,7 +983,7 @@ void Application::initUi() {
     m_fileDialogPopup.requestLayout();
   });
 
-  m_configService.addReloadCallback([]() { malloc_trim(0); });
+  // m_configService.addReloadCallback([]() { malloc_trim(0); });
 
   m_timeService.setTickSecondCallback([this]() {
     m_wallpaper.onSecondTick();
diff --git a/src/ipc/ipc_client.cpp b/src/ipc/ipc_client.cpp
index 2bae75f8d..fe3edab81 100644
--- a/src/ipc/ipc_client.cpp
+++ b/src/ipc/ipc_client.cpp
@@ -5,6 +5,8 @@
 #include <cstring>
 #include <sys/socket.h>
 #include <sys/un.h>
+#include <utmpx.h>
+#include <sys/time.h>
 #include <unistd.h>
 
 namespace {
diff --git a/src/ipc/ipc_service.cpp b/src/ipc/ipc_service.cpp
index 50825fab3..b38d22deb 100644
--- a/src/ipc/ipc_service.cpp
+++ b/src/ipc/ipc_service.cpp
@@ -8,6 +8,7 @@
 #include <sys/socket.h>
 #include <sys/un.h>
 #include <unistd.h>
+#include <utmpx.h>
 
 namespace {
 
diff --git a/src/render/core/thumbnail_service.cpp b/src/render/core/thumbnail_service.cpp
index 3e4675cda..062e532f1 100644
--- a/src/render/core/thumbnail_service.cpp
+++ b/src/render/core/thumbnail_service.cpp
@@ -74,7 +74,7 @@ namespace {
     }
 
     const auto ticks = mtime.time_since_epoch().count();
-    const std::string key = sourcePath + '\n' + std::to_string(size) + '\n' + std::to_string(ticks) + '\n' +
+    const std::string key = sourcePath + '\n' + std::to_string(size) + '\n' + std::to_string(static_cast<long long>(ticks)) + '\n' +
                             std::to_string(kThumbnailTargetPx) + '\n' + std::string(kThumbnailCacheVersion);
     return thumbnailCacheDir() / (hex64(fnv1a64(key)) + ".webp");
   }
diff --git a/src/scripting/luau_host.cpp b/src/scripting/luau_host.cpp
index b6237a5b9..ce449c430 100644
--- a/src/scripting/luau_host.cpp
+++ b/src/scripting/luau_host.cpp
@@ -11,11 +11,11 @@
 #include <string>
 
 namespace {
-  Logger log{"luau"};
+  Logger luau_logger{"luau"};
 
   int luau_log(lua_State* L) {
     const char* msg = luaL_checkstring(L, 1);
-    log.info("{}", msg);
+    luau_logger.info("{}", msg);
     return 0;
   }
 
@@ -107,7 +107,7 @@ bool LuauHost::loadString(std::string_view chunkName, std::string_view source) {
   size_t bytecodeSize = 0;
   char* bytecode = luau_compile(source.data(), source.size(), nullptr, &bytecodeSize);
   if (!bytecode) {
-    log.error("luau_compile returned null for chunk '{}'", std::string(chunkName));
+    luau_logger.error("luau_compile returned null for chunk '{}'", std::string(chunkName));
     return false;
   }
   std::string name(chunkName);
@@ -115,7 +115,7 @@ bool LuauHost::loadString(std::string_view chunkName, std::string_view source) {
   std::free(bytecode);
   if (loadResult != 0) {
     const char* err = lua_tostring(m_T, -1);
-    log.error("luau_load failed for '{}': {}", name, err ? err : "(no error)");
+    luau_logger.error("luau_load failed for '{}': {}", name, err ? err : "(no error)");
     lua_pop(m_T, 1);
     return false;
   }
@@ -126,7 +126,7 @@ bool LuauHost::run() {
   int rc = lua_pcall(m_T, 0, 0, 0);
   if (rc != 0) {
     const char* err = lua_tostring(m_T, -1);
-    log.error("lua_pcall failed: {}", err ? err : "(no error)");
+    luau_logger.error("lua_pcall failed: {}", err ? err : "(no error)");
     lua_pop(m_T, 1);
     return false;
   }
@@ -149,7 +149,7 @@ bool LuauHost::callGlobal(const char* name) {
   int rc = lua_pcall(m_T, 0, 0, 0);
   if (rc != 0) {
     const char* err = lua_tostring(m_T, -1);
-    log.error("call to '{}' failed: {}", name, err ? err : "(no error)");
+    luau_logger.error("call to '{}' failed: {}", name, err ? err : "(no error)");
     lua_pop(m_T, 1);
     return false;
   }
@@ -166,7 +166,7 @@ bool LuauHost::callGlobalWithBool(const char* name, bool value) {
   int rc = lua_pcall(m_T, 1, 0, 0);
   if (rc != 0) {
     const char* err = lua_tostring(m_T, -1);
-    log.error("call to '{}' failed: {}", name, err ? err : "(no error)");
+    luau_logger.error("call to '{}' failed: {}", name, err ? err : "(no error)");
     lua_pop(m_T, 1);
     return false;
   }
@@ -182,7 +182,7 @@ std::optional<std::string> LuauHost::callGlobalReturningString(const char* name)
   int rc = lua_pcall(m_T, 0, 1, 0);
   if (rc != 0) {
     const char* err = lua_tostring(m_T, -1);
-    log.error("call to '{}' failed: {}", name, err ? err : "(no error)");
+    luau_logger.error("call to '{}' failed: {}", name, err ? err : "(no error)");
     lua_pop(m_T, 1);
     return std::nullopt;
   }
diff --git a/src/shell/panel/panel_manager.cpp b/src/shell/panel/panel_manager.cpp
index b2057b606..b434ccdfd 100644
--- a/src/shell/panel/panel_manager.cpp
+++ b/src/shell/panel/panel_manager.cpp
@@ -691,7 +691,7 @@ void PanelManager::destroyPanel() {
     m_platform->stopKeyRepeat();
   }
 
-  malloc_trim(0);
+  // malloc_trim(0);
 }
 
 void PanelManager::togglePanel(const std::string& panelId, PanelOpenRequest request) {
diff --git a/src/time/time_format.cpp b/src/time/time_format.cpp
index d2de59106..d7e858731 100644
--- a/src/time/time_format.cpp
+++ b/src/time/time_format.cpp
@@ -154,7 +154,12 @@ std::string formatLocalTime(const char* fmt) {
     return *compat;
   }
 
-  const auto local = current_zone()->to_local(now);
+  // const auto local = current_zone()->to_local(now);
+  std::tm date;
+  time_t tn;
+  tn = std::chrono::system_clock::to_time_t(now);
+  localtime_r(&tn, &date);
+  const auto local = std::chrono::system_clock::from_time_t(timegm(&date));
   try {
     return std::vformat(std::locale(""), normalizedFmt, std::make_format_args(local));
   } catch (...) {
