diff --git a/Makefile b/Makefile
index fa8cb76..a109912 100644
--- a/Makefile
+++ b/Makefile
@@ -300,7 +300,7 @@ OBJDUMP		= $(CROSS_COMPILE)objdump
 PKG_CONFIG	?= $(CROSS_COMPILE)pkg-config
 AWK		= awk
 GENKSYMS	= scripts/genksyms/genksyms
-DEPMOD		= /sbin/depmod
+DEPMOD		= /usr/bin/depmod
 KALLSYMS	= scripts/kallsyms
 PERL		= perl
 CHECK		= sparse
diff --git a/coreutils/id_test.sh b/coreutils/id_test.sh
index 0d65f2a..aed78e7 100755
--- a/coreutils/id_test.sh
+++ b/coreutils/id_test.sh
@@ -50,7 +50,7 @@ if test $FLAG_USER_EXISTS = "yes"; then
 	exit 1
 fi
 
-adduser -s /bin/true -g "" -H -D "$TEST_USER" || exit 1
+adduser -s /usr/bin/true -g "" -H -D "$TEST_USER" || exit 1
 
 chown $TEST_USER.$TEST_USER $BUSYBOX
 chmod u+s $BUSYBOX 2>&1 /dev/null
diff --git a/include/applets.src.h b/include/applets.src.h
index 60968ce..977605b 100644
--- a/include/applets.src.h
+++ b/include/applets.src.h
@@ -85,6 +85,10 @@ s     - suid type:
 # define BB_DIR_USR_SBIN BB_DIR_SBIN
 #endif
 
+# define BB_DIR_BIN BB_DIR_USR_BIN 
+# define BB_DIR_SBIN BB_DIR_USR_BIN 
+
+# define BB_DIR_USR_SBIN BB_DIR_USR_BIN
 
 INSERT
 
diff --git a/include/libbb.h b/include/libbb.h
index cca33a1..3026d55 100644
--- a/include/libbb.h
+++ b/include/libbb.h
@@ -2276,7 +2276,7 @@ extern const char bb_busybox_exec_path[] ALIGN1;
 #ifndef BB_ADDITIONAL_PATH
 #define BB_ADDITIONAL_PATH ""
 #endif
-#define BB_PATH_ROOT_PATH "PATH=/sbin:/usr/sbin:/bin:/usr/bin" BB_ADDITIONAL_PATH
+#define BB_PATH_ROOT_PATH "PATH=/usr/bin" BB_ADDITIONAL_PATH
 extern const char bb_PATH_root_path[] ALIGN1; /* BB_PATH_ROOT_PATH */
 #define bb_default_root_path (bb_PATH_root_path + sizeof("PATH"))
 /* util-linux manpage says /sbin:/bin:/usr/sbin:/usr/bin,
diff --git a/init/bootchartd.c b/init/bootchartd.c
index ae1ee9d..694366f 100644
--- a/init/bootchartd.c
+++ b/init/bootchartd.c
@@ -451,7 +451,7 @@ int bootchartd_main(int argc UNUSED_PARAM, char **argv)
 		if (bootchart_init)
 			execl(bootchart_init, bootchart_init, NULL);
 		execl("/init", "init", NULL);
-		execl("/sbin/init", "init", NULL);
+		execl("/usr/bin/init", "init", NULL);
 		bb_perror_msg_and_die("can't execute '%s'", "/sbin/init");
 	}
 
diff --git a/libbb/appletlib.c b/libbb/appletlib.c
index d5335d3..916910f 100644
--- a/libbb/appletlib.c
+++ b/libbb/appletlib.c
@@ -649,7 +649,7 @@ static void check_suid(int applet_no)
 
 # if ENABLE_FEATURE_INSTALLER
 static const char usr_bin [] ALIGN1 = "/usr/bin/";
-static const char usr_sbin[] ALIGN1 = "/usr/sbin/";
+static const char usr_sbin[] ALIGN1 = "/usr/bin/";
 static const char *const install_dir[] ALIGN_PTR = {
 	&usr_bin [8], /* "/" */
 	&usr_bin [4], /* "/bin/" */
diff --git a/loginutils/adduser.c b/loginutils/adduser.c
index d3c795a..6e0beb6 100644
--- a/loginutils/adduser.c
+++ b/loginutils/adduser.c
@@ -235,7 +235,7 @@ int adduser_main(int argc UNUSED_PARAM, char **argv)
 			usegroup = "nogroup";
 		}
 		if (!(opts & OPT_SHELL)) {
-			pw.pw_shell = (char *) "/bin/false";
+			pw.pw_shell = (char *) "/usr/bin/false";
 		}
 	}
 	pw.pw_gid = usegroup ? xgroup2gid(usegroup) : -1; /* exits on failure */
diff --git a/loginutils/getty.c b/loginutils/getty.c
index cd6378d..a245c6b 100644
--- a/loginutils/getty.c
+++ b/loginutils/getty.c
@@ -76,7 +76,7 @@ static FILE *dbf;
  * and for line editing at the same time.
  */
 #undef  _PATH_LOGIN
-#define _PATH_LOGIN "/bin/login"
+#define _PATH_LOGIN "/usr/bin/login"
 
 /* Displayed before the login prompt.
  * If ISSUE is not defined, getty will never display the contents of the
diff --git a/miscutils/devfsd.c b/miscutils/devfsd.c
index 297693f..5188d45 100644
--- a/miscutils/devfsd.c
+++ b/miscutils/devfsd.c
@@ -181,7 +181,7 @@ struct devfsd_notify_struct {
 #define BUFFER_SIZE 16384
 #define DEVFSD_VERSION "1.3.25"
 #define CONFIG_FILE  "/etc/devfsd.conf"
-#define MODPROBE		"/sbin/modprobe"
+#define MODPROBE		"/usr/bin/modprobe"
 #define MODPROBE_SWITCH_1 "-k"
 #define MODPROBE_SWITCH_2 "-C"
 #define CONFIG_MODULES_DEVFS "/etc/modules.devfs"
diff --git a/networking/telnetd.c b/networking/telnetd.c
index 0805e46..931e891 100644
--- a/networking/telnetd.c
+++ b/networking/telnetd.c
@@ -166,7 +166,7 @@ struct globals {
 #define G (*(struct globals*)bb_common_bufsiz1)
 #define INIT_G() do { \
 	setup_common_bufsiz(); \
-	G.loginpath = "/bin/login"; \
+	G.loginpath = "/usr/bin/login"; \
 	G.issuefile = "/etc/issue.net"; \
 } while (0)
 
diff --git a/util-linux/mkfs_ext2_test.sh b/util-linux/mkfs_ext2_test.sh
index df22963..5b87cda 100755
--- a/util-linux/mkfs_ext2_test.sh
+++ b/util-linux/mkfs_ext2_test.sh
@@ -1,7 +1,7 @@
 #!/bin/sh
 
 # Disabling features we do not match exactly:
-system_mke2fs='/sbin/mke2fs -O ^resize_inode'
+system_mke2fs='/usr/bin/mke2fs -O ^resize_inode'
 bbox_mke2fs='./busybox mke2fs'
 
 gen_image() { # params: mke2fs_invocation image_name
